#include<bits/stdc++.h>

using namespace std;
int main()
{
    int a[]={1,2,3,4,5,6,7};
    int n=sizeof(a) / sizeof(a[0]);
    std::cout <<"last element"<<a[n-1]<<endl<<"first element" <<a[0];
    return 0;
}
