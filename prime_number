#include<iostream>

using namespace std;
int main()
{
	int a, b = 0;
	std::cin>>a;
	for( int i = 1 ; i <= a;i++)
	{
		if(a%i==0)
			b++;
	}

	if(b==2)
		std::cout << "its a prime";
	else
		std::cout << "not prime";
}
